import * as React from "react";

export interface TrTableDefaultProps {
  projectColor?: React.ReactNode;
  leaderStatus?: React.ReactNode;
  dropdownColor?: React.ReactNode;
}

export default class TrTableDefault extends React.Component<
  TrTableDefaultProps
> {}
