import * as React from "react";

export default class AsyncPagination extends React.Component {
  _handleInputChange(query: any): any;
  _handlePagination(e: any, shownResults: any): any;
  _handleSearch(query: any): any;
}
