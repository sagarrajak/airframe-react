import * as React from "react";

export default class RenderingExample extends React.Component {
  _renderMenu(results: any, menuProps: any): any;
  _renderMenuItemChildren(option: any, props: any): any;
  _renderToken(option: any, props: any, index: any): any;
}
