import * as React from "react";

export interface TasksMediaProps {
  iconColor?: React.ReactNode;
  id?: React.ReactNode;
}

export default class TasksMedia extends React.Component<TasksMediaProps> {}
