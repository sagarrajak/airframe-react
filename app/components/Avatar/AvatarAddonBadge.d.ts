import * as React from "react";

export interface AvatarAddonBadgeProps {
  children?: React.ReactNode;
  className?: string;
}

export default class AvatarAddonBadge extends React.Component<
  AvatarAddonBadgeProps
> {}
